/*import 'package:flutter_test/flutter_test.dart';
import 'package:projetomoderno/states/states_product.dart';

void main() {
  group('ProductState', () {
    test('addProduct should add a product to the list', () async {
      // Arrange
      final productState = ProductState();
      final product = {'id': '1', 'name': 'Product 1'};

      // Act
      productState.addProduct(product);

      // Assert
      expect(productState.products.length, 1);
      expect(productState.products['1'], product);
    });

    test('clearProduct should clear the product list', () async {
      // Arrange
      final productState = ProductState();
      final product = {'id': '1', 'name': 'Product 1'};
      productState.addProduct(product);

      // Act
      productState.clearProduct();

      // Assert
      expect(productState.products.length, 0);
    });
  });
}*/
